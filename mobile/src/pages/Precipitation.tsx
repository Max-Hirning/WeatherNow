import React, { ReactElement, } from 'react';
import { ScrollView, Text } from 'react-native';

export default function Precipitation(): ReactElement {
    return (
        <ScrollView className="bg-slate-700 px-4 py-4">
            <Text>sdvsdv</Text>
        </ScrollView>
    );
}